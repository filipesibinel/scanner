# Card Scanner Dependencies
# Tested with Python 3.12. For a CPU-only PyTorch (much smaller download), install it first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Web server (SocketIO runs in threading mode - no eventlet/gevent needed)
flask>=3.0
flask-socketio>=5.3

# Computer vision
numpy
opencv-python>=4.10
ultralytics>=8.3

# Image processing / HTTP / config
Pillow
requests
pyyaml>=6.0
python-dotenv

# Raspberry Pi Camera (optional - install via apt on the Pi: python3-picamera2)
