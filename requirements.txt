# Card Scanner dependencies (Python 3.10+)
# Optional YOLO fallback detector: see requirements-yolo.txt

# Web server (SocketIO runs in threading mode - no eventlet/gevent needed)
flask>=3.0
flask-socketio>=5.3

# Computer vision
numpy
opencv-python-headless>=4.10  # no GUI libraries needed (works on headless Pi OS Lite)

# Image processing / HTTP / config
Pillow
requests
pyyaml>=6.0
python-dotenv

# Raspberry Pi Camera (optional - install via apt on the Pi: python3-picamera2)
